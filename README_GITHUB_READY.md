# GitHub 上传整理说明

这份包主要做了这些整理：

- 重写 `README.md`，改成适合公开仓库展示的版本
- 扩展 `.gitignore`，避免把依赖、构建产物、数据库、备份包、日志提交上去
- 新增 `.gitattributes`，统一文本换行
- 保留原有工程结构与业务代码

建议你上传前再检查一次：
1. 有没有真实数据库文件
2. 有没有备份 zip
3. 有没有密码、私钥、助记词、测试账号
4. 有没有本地截图或临时文件

如果仓库准备公开，建议下一步再补一个 LICENSE。
