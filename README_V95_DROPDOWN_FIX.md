# V95 dropdown fix

This package fixes the toolbar dropdown regression introduced by preventing mouse down on native selects.

## Fixes
- native toolbar dropdowns are clickable again
- selection is remembered on select mouse down capture
- editor selection is still restored when formatting is applied
- keeps exact working Tauri version pins
